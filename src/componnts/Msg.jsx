import React from 'react';

const Msg = () => {
  const name = '';
  let nd_name = 'World';
  //   return (

  if (name) return <h1>Hello {name}</h1>;
  return <h1>Hello{nd_name}</h1>;

  //   );
};

export default Msg;
